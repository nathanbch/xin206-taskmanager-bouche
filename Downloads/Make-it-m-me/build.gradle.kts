// build.gradle.kts (Project level - MaSuperAppFirebase/build.gradle.kts)
// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id("com.android.application") version "8.1.1" apply false // Vérifiez la dernière version
    id("org.jetbrains.kotlin.android") version "1.8.10" apply false // Vérifiez la dernière version
    // Ajoute le plugin Google Services (vérifiez la dernière version)
    id("com.google.gms.google-services") version "4.4.2" apply false
}